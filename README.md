# GdeGruz - Telegram бот для отслеживания водителей 🚚

Система отслеживания местоположения водителей и управления рейсами на базе Telegram бота.

## 🚀 Быстрый старт

### Для разработчиков

```bash
# 1. Клонировать репозиторий
git clone <repo-url>
cd GdeGruz

# 2. Создать .env
cp .env.example .env
# Отредактировать .env (добавить BOT_TOKEN, CURATOR_IDS и т.д.)

# 3. Запустить
docker-compose up -d

# 4. Смотреть логи
docker-compose logs -f bot
```

### Команды Docker (на сервере)

**ВАЖНО**: Используется `docker-compose` (с дефисом)!

```bash
# Просмотр состояния
docker-compose ps
docker-compose logs --tail=50 bot

# Перезапуск
docker-compose restart bot

# Полный перезапуск с пересборкой
docker-compose down
docker-compose up -d --build

# Выполнение скриптов
docker-compose run --rm bot python urgent_cleanup.py
```

## 📚 Документация

### Для новой сессии Claude Code:

👉 **[ОПИСАНИЕ_ПРОЕКТА.md](ОПИСАНИЕ_ПРОЕКТА.md)** - Полное описание проекта для разработки

Содержит:
- Архитектуру проекта и структуру
- Описание всех модулей и БД
- Систему ролей (куратор/водитель)
- Все команды Docker для работы на сервере
- Процесс разработки и деплоя
- Типичные задачи и отладку

### Дополнительно:

- **[QUICK_START.md](QUICK_START.md)** - Быстрое решение проблем с БД
- **[ТЕХНИЧЕСКОЕ_ЗАДАНИЕ.md](ТЕХНИЧЕСКОЕ_ЗАДАНИЕ.md)** - Детальное ТЗ для устранения проблем

## 🎯 Основной функционал

### Для водителей:
- 📍 Отправка геолокации (напоминания каждые 12 часов)
- 📋 Просмотр своих рейсов
- 📤 Загрузка документов (фото, ТТН, УПД)
- 🚚 Изменение статуса рейса

### Для кураторов:
- ➕ Создание рейсов для водителей
- 📊 Просмотр всех рейсов и статистики
- ✅ Активация и завершение рейсов
- 📍 Запрос местоположения у водителей
- 📋 Просмотр загруженных документов

### Автоматика:
- ⏰ Напоминания водителям каждые 12 часов
- 🚨 Эскалация в группу при отсутствии связи 14+ часов
- 📢 Уведомления кураторов о событиях

## 🛠 Технологии

- **Python 3.11** + **Aiogram 3.0.0** (Telegram bot)
- **FastAPI** (веб-API для карты)
- **SQLite** (3 БД: points, trips, documents)
- **Docker** + **docker-compose**
- **GitHub Actions** (автодеплой)

## 📁 Структура проекта

```
GdeGruz/
├── bot/                    # Telegram бот
│   ├── main.py            # Точка входа, напоминания
│   ├── keyboards.py       # Клавиатуры (куратор/водитель)
│   ├── utils.py           # is_curator() - проверка роли
│   └── handlers/          # Обработчики событий
│       ├── start.py       # /start
│       ├── contact.py     # Регистрация
│       ├── curator.py     # Панель кураторов
│       └── ...
├── web/                    # FastAPI веб-сервер
│   └── api.py
├── db.py                   # points.db (водители, координаты)
├── db_trips.py            # trips.db (рейсы, события)
├── db_documents.py        # documents.db (загруженные файлы)
├── docker-compose.yml     # Конфигурация Docker
├── .env.example           # Пример переменных окружения
└── .github/workflows/     # GitHub Actions
    └── deploy.yml         # Автодеплой
```

## ⚙️ Настройка

### Переменные окружения (.env)

```bash
# Telegram
BOT_TOKEN=your_bot_token_here
ADMIN_ID=123456789
GROUP_CHAT_ID=-1001234567890
DOCUMENTS_GROUP_ID=-1001234567890

# Кураторы (узнать ID: отправить боту /myid)
CURATOR_IDS=5799866832,6835069941

# Настройки
REMIND_HOURS=12.0
TIMEZONE=Europe/Berlin
```

### Как узнать свой Telegram ID?

Отправьте боту команду `/myid`

### Как добавить нового куратора?

1. Попросите пользователя отправить боту `/myid`
2. Добавьте ID в `.env`: `CURATOR_IDS=123,456,NEW_ID`
3. Перезапустите: `docker-compose restart bot`

## 🚀 Деплой

### Автоматический (через GitHub)

1. Push в ветку `main`
2. GitHub Actions автоматически деплоит на сервер

### Вручную на сервере

```bash
ssh username@host
cd /home/git/fleet-live-bot

git pull origin main
docker-compose down
docker-compose up -d --build
```

## 🔧 Типичные задачи

### Очистить БД рейсов

```bash
docker-compose run --rm bot python urgent_cleanup.py
```

### Посмотреть рейсы в БД

```bash
sqlite3 data/trips.db "SELECT trip_number, status, phone FROM trips ORDER BY created_at DESC LIMIT 10;"
```

### Проверить логи

```bash
docker-compose logs --tail=100 bot
docker-compose logs -f bot  # следить в реальном времени
```

### Перезапустить бота

```bash
docker-compose restart bot
```

## 🐛 Отладка

### Бот не отвечает?

```bash
docker-compose ps                 # Проверить статус
docker-compose logs --tail=100 bot  # Посмотреть логи
docker-compose restart bot        # Перезапустить
```

### Куратор не видит админ-панель?

1. Узнать user_id: отправить боту `/myid`
2. Проверить `.env`: `cat .env | grep CURATOR_IDS`
3. Добавить ID и перезапустить: `docker-compose restart bot`

### Рейсы не сохраняются?

```bash
ls -lah data/trips.db                        # БД существует?
sqlite3 data/trips.db "SELECT COUNT(*) FROM trips;"  # Есть записи?
docker-compose logs bot | grep "Created trip"        # Логи создания
```

## 🔒 Безопасность

Исправленные уязвимости:
- ✅ RCE через /redeploy (отключена)
- ✅ API Token в frontend (HttpOnly cookies)
- ✅ Hardcoded CURATOR_IDS (читается из .env)
- ✅ Race conditions в эскалациях (asyncio.Lock)
- ✅ Кураторы получали напоминания о геолокации (исправлено)

## 📞 Поддержка

- **GitHub**: Tehnologistika/GdeGruz
- **Документация**: См. [ОПИСАНИЕ_ПРОЕКТА.md](ОПИСАНИЕ_ПРОЕКТА.md)

---

**Разработано с помощью Claude Code** 🤖
