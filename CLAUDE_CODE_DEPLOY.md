# 🤖 Автоматический деплой через Claude Code

## Как это работает

Теперь вы можете деплоить изменения **автоматически** через Claude Code, даже с телефона!

### Процесс автоматического деплоя:

```
Claude Code (любое устройство)
    ↓
Вносите изменения в код
    ↓
Push в ветку claude/*
    ↓
GitHub Actions автоматически:
  1. Создает Pull Request
  2. Мержит PR в main
  3. Запускает деплой на сервер
    ↓
Бот обновлен на сервере! ✅
```

## Использование

### Вариант 1: Через Claude Code (рекомендуется)

Просто работайте с Claude Code как обычно:

1. **Вносите изменения** (Claude сделает это за вас)
2. **Claude автоматически коммитит** в ветку `claude/*`
3. **Claude пушит в GitHub**
4. **GitHub Actions делает все остальное автоматически!**

Вам больше ничего не нужно делать! 🎉

### Вариант 2: Вручную (для особых случаев)

```bash
# Внесите изменения в код
git add .
git commit -m "Описание изменений"

# Запушьте в любую ветку claude/*
git push origin claude/my-feature

# Все! GitHub Actions автоматически смержит и задеплоит
```

## Что происходит автоматически

1. **Auto-merge workflow** (`.github/workflows/auto-merge-claude.yml`):
   - Обнаруживает push в ветку `claude/*`
   - Создает Pull Request в `main`
   - Автоматически мержит PR
   - Удаляет ветку `claude/*` после merge

2. **Deploy workflow** (`.github/workflows/deploy.yml`):
   - Срабатывает на push в `main`
   - Подключается к серверу по SSH
   - Пулит последние изменения
   - Проверяет флаг `.cleanup_db_on_deploy` (если нужно очистить БД)
   - Пересобирает Docker контейнеры
   - Перезапускает бот

## Очистка базы данных

Если нужно очистить тестовые данные при деплое:

1. **Создайте флаг-файл:**
   ```bash
   touch .cleanup_db_on_deploy
   git add .cleanup_db_on_deploy
   git commit -m "Очистка тестовых данных"
   git push
   ```

2. **При деплое автоматически:**
   - Удалятся все рейсы
   - Удалятся все документы
   - Водители останутся зарегистрированными
   - Флаг удалится автоматически

## Безопасность

✅ **Что защищено:**
- Ветка `main` защищена от прямого push
- Все изменения проходят через Pull Request
- История коммитов сохраняется
- Ветки `claude/*` автоматически удаляются после merge

✅ **Права доступа:**
- Только ветки с префиксом `claude/` мержатся автоматически
- Требуются права на GitHub репозиторий
- Деплой происходит только на ваш сервер

## Мониторинг

### Проверить статус деплоя:

1. **GitHub Actions:**
   ```
   https://github.com/Tehnologistika/GdeGruz/actions
   ```

2. **Логи на сервере:**
   ```bash
   ssh user@server
   cd /home/git/fleet-live-bot
   docker-compose logs bot --tail=50
   ```

### Проверить что бот работает:

```bash
docker-compose ps
```

Должно быть `running` статус.

## Откат изменений

Если что-то пошло не так:

```bash
ssh user@server
cd /home/git/fleet-live-bot

# Откатиться на предыдущий коммит
git log --oneline -5
git reset --hard <previous-commit-hash>

# Перезапустить контейнеры
docker-compose up -d --build
```

## Примеры использования

### Пример 1: Исправление бага

Claude Code делает все автоматически:
1. Вы: "Исправь баг в обработчике контактов"
2. Claude: вносит изменения, коммитит, пушит
3. GitHub: автоматически деплоит
4. Готово! ✅

### Пример 2: Добавление новой функции

```
Вы: "Добавь команду /stats для показа статистики"
Claude: создает код, тесты, документацию
Claude: git commit + git push
GitHub Actions: auto-merge + deploy
Функция работает на проде! ✅
```

### Пример 3: Очистка БД

```
Вы: "Очисти все тестовые данные"
Claude: создает .cleanup_db_on_deploy
Claude: git commit + git push
GitHub Actions: деплоит + очищает БД
БД чистая! ✅
```

## Troubleshooting

### Деплой не запустился

Проверьте:
1. Название ветки начинается с `claude/`
2. Есть права на репозиторий
3. GitHub Actions включены в настройках

### PR не создается автоматически

Проверьте:
1. GitHub Actions имеет права на создание PR
2. Нет конфликтов с `main`
3. Логи workflow в GitHub Actions

### Бот не перезапустился

```bash
ssh user@server
cd /home/git/fleet-live-bot
docker-compose logs bot --tail=100
```

Проверьте логи на ошибки.

## Поддержка

Если нужна помощь - обратитесь к Claude Code! 🤖

Команды для Claude:
- "Проверь почему деплой не сработал"
- "Посмотри логи последнего деплоя"
- "Откати последние изменения"
