# 🔧 Настройка ID группы документов

ID группы документов: **-5054329274**

## Вариант 1: Автоматическое обновление (рекомендуется)

На сервере выполните:

```bash
cd /home/git/fleet-live-bot
./update_documents_group.sh
```

Скрипт автоматически:
- Добавит DOCUMENTS_GROUP_ID в .env
- Перезапустит бота
- Покажет логи

## Вариант 2: Ручное обновление

### Шаг 1: Подключитесь к серверу

```bash
ssh user@your-server
```

### Шаг 2: Откройте .env файл

```bash
cd /home/git/fleet-live-bot
nano .env
```

### Шаг 3: Добавьте в конец файла

```env
# ID группы для документов
DOCUMENTS_GROUP_ID=-5054329274
```

### Шаг 4: Сохраните и закройте

- Нажмите `Ctrl+O` для сохранения
- Нажмите `Enter` для подтверждения
- Нажмите `Ctrl+X` для выхода

### Шаг 5: Перезапустите бота

```bash
cd /home/git/fleet-live-bot
docker compose restart bot
```

### Шаг 6: Проверьте логи

```bash
docker logs fleet-live-bot_bot_1 -f
```

Должны увидеть:
```
INFO - Documents database initialized at /home/git/fleet-live-bot/userdata/documents.db
```

## Проверка работы

После перезапуска бота:

1. Откройте бота в Telegram
2. Нажмите "📤 Отправить документы"
3. Выберите тип документа
4. Отправьте фото
5. Проверьте группу документов - должно появиться сообщение с фото и метаданными

## Устранение проблем

### Бот не отправляет в группу

Проверьте:
1. Бот добавлен в группу как администратор
2. ID группы правильный (должен начинаться с минуса)
3. Переменная DOCUMENTS_GROUP_ID установлена в .env
4. Бот перезапущен после изменения .env

### Проверить текущее значение

```bash
cat /home/git/fleet-live-bot/.env | grep DOCUMENTS_GROUP_ID
```

### Посмотреть логи

```bash
# Последние 100 строк
docker logs fleet-live-bot_bot_1 --tail 100

# В реальном времени
docker logs fleet-live-bot_bot_1 -f
```

## Проверка БД

```bash
# Посмотреть все документы
sqlite3 /home/git/fleet-live-bot/userdata/documents.db "SELECT * FROM documents;"

# Посмотреть файлы
ls -la /home/git/fleet-live-bot/userdata/documents/
```
