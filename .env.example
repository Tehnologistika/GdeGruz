# =============================================================================
# GdeGruz Bot Configuration
# =============================================================================
# Скопируйте этот файл в .env и заполните своими значениями
#
# Для получения BOT_TOKEN:
# 1. Откройте Telegram и найдите @BotFather
# 2. Отправьте /newbot и следуйте инструкциям
# 3. Скопируйте полученный токен сюда
# =============================================================================

# ----- ОБЯЗАТЕЛЬНЫЕ ПАРАМЕТРЫ -----

# Токен Telegram бота (получить у @BotFather)
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# ID администратора бота (ваш Telegram user ID)
# Получить можно у @userinfobot
ADMIN_ID=123456789

# ID группового чата для уведомлений диспетчерской
# Создайте группу, добавьте бота и @userinfobot, чтобы узнать ID
GROUP_CHAT_ID=-1001234567890


# ----- ДОПОЛНИТЕЛЬНЫЕ ПАРАМЕТРЫ -----

# Интервал напоминаний в часах (по умолчанию 0.2 = 12 минут)
# Для продакшена рекомендуется 12-24 часа
REMIND_HOURS=12

# Часовой пояс (по умолчанию Europe/Moscow)
TIMEZONE=Europe/Moscow

# Путь к базе данных (по умолчанию /app/data/points.db для Docker)
# Для локальной разработки можно использовать ./data/points.db
DB_PATH=/app/data/points.db


# ----- НАСТРОЙКИ WEB API -----

# Секретный токен для доступа к API
# Сгенерируйте надежный токен, например: openssl rand -base64 32
API_SECRET_TOKEN=your-secret-token-here-change-me


# ----- ПРИМЕЧАНИЯ -----
#
# После изменения .env файла перезапустите контейнеры:
#   docker compose down && docker compose up -d
#
# Для локальной разработки используйте:
#   python -m bot.main
#
